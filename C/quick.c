#include<stdio.h>

int main(){
    int a=10; // Start from 10
    while (a<=20) {
        printf("the natural numbers from 10 to 20 are %d\n",a);
        a++; // Increment a inside the loop
    }
    
    return 0;
}
