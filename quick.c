#include<stdio.h>

int main(){
    int a=0;
    while (a<=20) {
        if (a>=10)
        {
            printf("the natural numbers from 10 to 20 are %d",a);
        }
        a++;
    }
    
    return 0;
}